# Interfaccia Interna (Verso lo Switch/LAN)

ip address add 10.0.0.254/24 dev eth0

# Interfaccia Esterna (Verso Internet)

ip address add 203.0.113.1/24 dev eth1

# Abilita il NAT (Masquerading) per uscire su Internet

iptables -t nat -A POSTROUTING -o eth1 -j MASQUERADE